apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: plex
  namespace: media
spec:
  interval: 30s
  chart:
    spec:
      chart: plex
      version: 0.1.8
      sourceRef:
        kind: HelmRepository
        name: plex
        namespace: flux-system
  maxHistory: 5
  install:
    remediation:
      retries: 5
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 5
  uninstall:
    keepHistory: false
  dependsOn:
    - name: longhorn
      namespace: kube-system
  values:
    config:
    env:
      PLEX_CLAIM: claim--xPeaBsx4FDxar43otF1
    persistence:
      config:
        enabled: false
        mountPath: /config
      transcode:
        enabled: false
        mountPath: /transcode
      data:
        enabled: true
        existingClaim: media-pvc

    ingress:
      main:
        enabled: "true"
        ingressClassName: internal
        annotations:
          "plex.io/enable": "true"
        hosts:
          - host: &host "plex.${SECRET_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host

